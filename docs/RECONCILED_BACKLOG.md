# EnglishKolab — Reconciled Backlog (verified vs current code)

> Generated 2026-06-09 by the `reconcile-backlogs` workflow: 12 agents read every QA/feature doc and cross-checked each item against the CURRENT shipped code (through commit 9701618 / migration 033). This is the single source of truth — supersedes the memory-based summaries.

## Scorecard

| Status | Count |
|---|---:|
| done | 105 |
| pending | 110 |
| partial | 19 |
| needs-live-check | 13 |
| needs-you-design | 4 |
| **total** | **251** |

**Actionable (pending+partial): 129** — high 21, medium 42, low 61, feature 4, design 1.

> **Progress (fix-everything run):** Batches A–I shipped & deployed (2026-06-09), ~85/129 closed, ALL high-sev done. Batch I closed: BOOKING-05 (interval-overlap conflict via new `lib/bookingConflict.ts`, replaces exact-timestamp checks in createBooking/studentRescheduleBooking/bookPlacementCall), ONBOARD-04 (CV magic-byte sniff), TEACH-LOW-1 (updateTeacherProfile teacher-row guard). Batch H closed: BOOKING-06, BOOKING-08, HOMEWORK-01, HOMEWORK-02, I18N-02, LIB-02, DASH-04 (slot grid now built in profiles.timezone via new `lib/timezone.ts` zoned-time helpers). Batch G closed: CALL-02, CALL-03, CALL-05, CALL-06, LIB-01, LIB-03, LIB-04, LIB-06. Batch F closed: BOOK-04, BOOK-05, CALL-01, E8-BILINGUAL-ASSIGNMENT-EMAIL, E9-BILINGUAL-APPROVAL-EMAIL, STRIPE-04, ONBOARD-06, ASSIGN-05, Payments-LOW-stripe-errors, Payments-LOW-lang-param. New deferred (design/copy, needs Carlos): placement-complete UI state — a placement-done student still sees a "Reschedule call" CTA that now correctly errors against the BOOK-05 server guard; gate it on `placement_test_done` in `placement/page.tsx` + `PlacementScheduledScreen.tsx`.

## Pending + Partial — actionable (I can fix + verify)

| ✓ | id | sev | surface | item | fix hint |
|---|---|---|---|---|---|
| ◻ | Payments-HIGH-refund-idempotent | high | Payments / Stripe | Refund decrement not idempotent per charge — multiple charge.refunded events re-subtract | Verify whether a single charge.refunded event can fire twice. If so, add a per-charge refund tracker (processed_refunds table) to idempotent |
| ◻ | BOOKING-01 | high | Booking flow (HIGH), Student dashboard ( | createBooking ignores decrement_classes() return value → credit not actually decremented | src/app/actions/booking.ts:135-150: Check the boolean result from decrement_classes and return an error if false. Alternatively, call decrem |
| ◻ | BOOKING-02 | high | Booking flow (HIGH), Teacher (HIGH), Aut | confirmBooking has no status guard → can resurrect cancelled/declined bookings | src/app/actions/booking.ts:185-189: Add `.eq('status', 'pending')` to the update filter, or read the status first and bail if not pending. |
| ◻ | BOOKING-03 | high | Booking flow (HIGH), Student dashboard ( | Student/no-show cancel + concurrent student cancel double-refund credit | src/app/actions/booking.ts:427-436 and 616-627: Rewrite as a single atomic RPC that updates status by (id AND status IN ('pending','confirme |
| ◻ | ADMIN-01 | high | Admin CRM (HIGH) | cancelBookingWithRefund has no status guard → credit inflation on repeated calls | src/app/[lang]/admin/actions.ts:545: Add `.eq('status', 'pending').eq('status', 'confirmed')` filter (or `.in('status', ['pending', 'confirm |
| ◻ | AUTH-01 | high | Auth & session (HIGH) | Rate limiter trusts first hop of X-Forwarded-For; no per-account lockout |  |
| ◻ | TEACH-HIGH-2 | high | Teacher (maestro) | confirmBooking has no status guard → resurrect cancelled/declined booking | Add .eq('status', 'pending') gate at line 189. Consider optional DB CHECK constraint or trigger to enforce status machine. |
| ◻ | ADMIN-01 | high | Admin CRM | cancelBookingWithRefund missing status guard allows repeated refunds | Line 545-550: Add status guard to UPDATE clause: .eq('id', bookingId).in('status', ['pending', 'confirmed']) before unconditional increment_ |
| ◻ | ADMIN-02 | high | Admin CRM | addStudentClasses non-atomic, never calls add_classes RPC | Line 454-457: Replace SELECT + UPDATE with await admin.rpc('add_classes', { p_student_id: studentId, p_count: count }); |
| ◻ | AUTH-01 | high | Authorization / RLS / IDOR | confirmBooking teacher-side has no status guard, resurrects cancelled bookings | Line 185-189: Add status guard: .eq('status', 'pending') to the UPDATE filter chain. |
| ◻ | ASSIGN-01 | high | Assignments / homework | Deactivated/pending teacher (is_active=false) can create/grade/cancel assignments via serv | src/app/actions/assignments.ts:20 — add 'is_active' to the select() call and check: if (!teacher?.is_active) return { error: 'Teacher accoun |
| ◻ | PRICE-01 | high | Public landing / marketing | Non-USD prices render at USD magnitude on first paint until FX rates load — understates pr | src/components/landing/Pricing.tsx:82-83 — add const { loading } = useCurrency() (already returned); gate price rendering: while loading OR  |
| ◻ | ADMIN-01 | high | Admin (2nd pass) | Admin-created class booking never decrements credit, but cancel-refund mints one — free cr | src/app/[lang]/admin/actions.ts:695-706 — for type==='class', call admin.rpc('decrement_classes', { p_student_id: studentId }) after booking |
| ◻ | ADMIN-02 | high | Admin (2nd pass) | setStudentDeactivated has no role check and no self-ban guard, and there's no reactivation | src/app/[lang]/admin/actions.ts:607-619 — reject when profileId === acting admin's id; require target's role to be student or block banning  |
| ◻ | LIVE-001 | high | Admin booking assignment (drag-drop + de | Admin 'Assign anyway?' override unreachable in production | Change assignAndConfirmBooking() in src/app/[lang]/admin/actions.ts to return a structured result like { ok: false, reason: 'not_available'  |
| ◻ | ST-04 | high | Settings > Notifications tab; scheduled  | Reminders / notifications — Email + SMS + WhatsApp (all three) | Email reminders: verify Resend scheduled-delivery works (migration 023 removed cron, uses scheduleBookingReminders). SMS: add Twilio/alt int |
| ◻ | ST-11 | high | My Progress page — 'Download report (PDF | PDF report download is broken | Create a server action src/app/actions/reports.ts → generateProgressReport(studentId, lang) that queries student stats, completed sessions,  |
| ◻ | TE-03 | high | Availability (Disponibilidad) page — wee | Weekly-summary slot count looks off / unclear — decide window vs hourly slots model | src/app/[lang]/maestro/dashboard/disponibilidad/AvailabilityClient.tsx:550: Consider renaming the display from 'slots' to 'availability wind |
| ◻ | TE-04 | high | Earnings (Ganancias) page — teacher payo | Rebuild Earnings as an Upwork-style payouts experience (big rock) | Implement in phases: (1) Fetch teacher balance + pending balance from Stripe API (stripe.balance.retrieve + balance_transactions); (2) Add U |
| ◻ | AD-02 | high | Admin bookings calendar view | Bookings can be missed — calendar too big, bookings overlooked | AD-03 (Google Calendar-style redesign) is the main ticket to address this. However, an interim fix: consider starting the view at 6am (busin |
| ◻ | AD-03 | high | Admin bookings calendar (replacement of  | Build a Google-Calendar-style bookings calendar with multi-teacher overlay, out-of-office  | Decide: adopt a library (FullCalendar Pro / react-big-calendar) vs. rebuild custom. If custom, extend BookingCalendarClient to: (1) scroll v |
| ◻ | Auth-MEDIUM-signin-errors | medium | Auth & session | signIn reflects raw Supabase error strings into URL/UI (info leak + i18n gap) | src/app/actions/auth.ts:160-189. Map signin errors to a fixed set of localized codes (e.g., 'invalid_credentials', 'user_not_found', 'email_ |
| ◻ | Payments-MEDIUM-concurrent-webhook | medium | Payments / Stripe | Concurrent webhook credit non-atomic read-then-write | Call an atomic RPC (e.g., add_classes or increment_classes SECURITY DEFINER) to atomically increment rather than read-then-write. |
| ◻ | Payments-MEDIUM-current-plan-refund | medium | Payments / Stripe | Full refund never clears current_plan | src/app/api/stripe/webhook/route.ts:189-199. When newCount reaches 0 after refund decrement, also set current_plan = NULL in the same update |
| ◻ | Payments-MEDIUM-subscriptions-payments-rows | medium | Payments / Stripe | Webhook never writes subscriptions/payments rows — billing history permanently empty | After crediting classes (line 154-160), insert a row into a payments table with session_id, user_id, plan_key, amount_usd, created_at for bi |
| ◻ | DASH-01 | medium | Student dashboard & nav (MEDIUM) | updateStudentProfile accepts arbitrary timezone string with no validation → RangeError in  | src/app/actions/profile.ts:46: Validate timezone against `Intl.supportedValuesOf('timeZone')` before patching. Also wrap formatTime/dayInTz  |
| ✓ | DASH-04 | medium | Booking flow (MEDIUM) | Agendar slot grid uses browser timezone (Date.setHours), rest of app uses profiles.timezon | src/app/[lang]/dashboard/agendar/AgendarClient.tsx:getWeekDates and cellDate.setHours: Build the week and grid hours in the canonical profil |
| ◻ | DASH-05 | medium | Student dashboard & nav (MEDIUM) | Placement booking/reschedule skip date validation (unlike createBooking) | src/app/actions/placement.ts:29, 117: Add 24-hour advance-notice check + NaN guard (same guards as createBooking lines 85-94). |
| ◻ | DASH-07 | medium | Student dashboard & nav (MEDIUM) | No rate limiting on student mutations (booking, placement, profile, email-change) | Extend rateLimit.ts to include 'createBooking', 'updateProfile', 'requestEmailChange' actions. Guard the sensitive student mutations with ch |
| ◻ | BOOKING-04 | medium | Booking flow (MEDIUM) | createBooking accepts Invalid Date → 24h guard silently passes (NaN < minAllowed = false) | src/app/actions/booking.ts:86-87: Add `if (isNaN(scheduledDate.getTime())) return { error: '...' }` before the minAllowed comparison. |
| ◻ | LIVE-SALA-MED-2 | medium | Live call (sala) | completeSession increment + payment insert not atomic | Add early guard: if (booking.status !== 'confirmed') return { error: '...' } before line 334. Consider unique constraint on payments.booking |
| ◻ | TEACH-MED-1 | medium | Teacher (maestro) | saveAvailabilitySlots zero server validation + non-atomic delete-then-insert | Validate each slot: day_of_week ∈ [0,6], time format (HH:MM), start < end, cap array length. Reject entire batch on any invalid row. Make re |
| ◻ | TEACH-MED-2 | medium | Teacher (maestro) | Booking mutations don't enforce is_active | Add .select('id, is_active') and guard: if (!teacher.is_active) return { error: 'Teacher account is not active' }. Create a helper requireAc |
| ◻ | ADMIN-03 | medium | Admin CRM | completeBooking has no status guard, resurrects cancelled bookings | Line 521: Add status guard: .eq('id', bookingId).eq('status', 'confirmed') before UPDATE. |
| ◻ | ADMIN-04 | medium | Admin CRM | approveRescheduleRequest moves booking with no availability/conflict re-check | Line 299-303: Before UPDATE, load the booking's teacher_id, then call isTeacherAvailable() and teacherHasConflict() guards (same as assignAn |
| ◻ | ADMIN-05 | medium | Admin CRM | createAdminBooking skips availability/conflict checks when teacher pre-assigned | Line 690-693: Add availability/conflict guards (same as assignAndConfirmBooking) if !force. Line 708: Add 23505 catch on INSERT error. |
| ◻ | ADMIN-06 | medium | Admin CRM | updateStudentRole accepts arbitrary role string, no allowlist | Line 600: Add allowlist: if (!['student', 'teacher', 'admin'].includes(role)) throw; check if profileId === self, block if only admin remain |
| ◻ | ADMIN-07 | medium | Admin CRM | resetStudentPassword takes arbitrary caller-supplied email, not tied to student | Change signature to resetStudentPassword(studentId: string). Load student server-side via admin client, validate target, rate-limit per stud |
| ◻ | I18N-01 | medium | i18n / routing / responsive / error-stat | No error.tsx, not-found.tsx, or global-error.tsx boundaries exist | Create src/app/[lang]/error.tsx, src/app/[lang]/not-found.tsx (localized), and src/app/global-error.tsx (top-level). Wire error boundaries p |
| ✓ | I18N-02 | medium | i18n / routing / responsive / error-stat | Language toggle in Navbar/Sidebar drops query parameters | Line 43 (Navbar), Line 102 (Sidebar): Use useSearchParams(); append to otherLocalePath: const otherLocalePath = pathname.replace(...) + (sea |
| ◻ | ASSIGN-02 | medium | Assignments / homework | createAssignment has no server-side length cap on title/instructions or validation of dueA | src/app/actions/assignments.ts:45-66 — add: if (title.length > 120) return error; if (!instructions.trim()) return error; cap instructions ≤ |
| ◻ | ASSIGN-03 | medium | Assignments / homework | gradeSubmission permits empty feedback + 'No score' permanently locking student to read-on | src/app/actions/assignments.ts:156 — add: if (!feedback.trim() && !score) return { error: 'Feedback required or score must be set' }. Cap fe |
| ◻ | ASSIGN-05 | medium | Assignments / homework | Assignment server actions leak raw Postgres/Supabase error strings + English-only messages | src/app/actions/assignments.ts — map error.message to localized keys; detect 23505 on submit and return localized 'already received' message |
| ◻ | ONBOARD-01 | medium | Onboarding + Intake | completeStudentOnboarding / completeTeacherOnboarding have NO role check — any authed user | src/app/actions/onboarding.ts:13-15 and 50-53 — after auth check, fetch profiles.role and verify it matches the expected role. Reject if rol |
| ◻ | ONBOARD-02 | medium | Onboarding + Intake | Re-invoking completeTeacherOnboarding upserts is_active=false/hourly_rate=0 and overwrites | src/app/actions/onboarding.ts:88-100 — check if teachers row already exists before upsert. If exists, update only application fields (bio, s |
| ◻ | AUTH-01 | medium | Auth & session (2nd pass) | OAuth (Google/Microsoft) sign-in is dead code — handlers defined but no JSX ever calls the | src/app/[lang]/login/page.tsx and registro/page.tsx — either render OAuth buttons (onClick={() => handleOAuth('google|azure')}, show oauthEr |
| ◻ | AUTH-02 | medium | Auth & session (2nd pass) | Signup silently discards user's detected timezone — every new account defaults to America/ | src/app/actions/auth.ts:122 — write timezone alongside phone in the post-signup UPDATE, or extend handle_new_user to coalesce raw_user_meta_ |
| ◻ | AUTH-03 | medium | Auth & session (2nd pass) | Signup discards preferred_language — English signups get Spanish transactional emails unti | src/app/actions/auth.ts:122 — write preferred_language alongside phone/timezone in post-create UPDATE, or coalesce raw_user_meta_data->>'pre |
| ◻ | BOOK-03 | medium | Booking (2nd pass) | reportTeacherNoShow mints paid-class credit for free placement booking (no type guard) | src/app/actions/booking.ts:550 — add booking.type to select; only call increment_classes when booking.type === 'class'. For placements, canc |
| ◻ | BOOK-04 | medium | Booking (2nd pass) | Student reschedule/cancel leaves orphaned pending reschedule_request admin can still appro | src/app/actions/booking.ts:437,361 — when student reschedules/cancels, mark open reschedule_requests for that booking_id as cancelled. src/a |
| ◻ | BOOK-05 | medium | Booking (2nd pass) | reschedulePlacementCall has no completed/done guard — direct call re-opens a finished plac | src/app/actions/placement.ts:116-176 — bail if placement_test_done is true; only cancel placement bookings whose status is in (pending, conf |
| ◻ | ADMIN-03 | medium | Admin (2nd pass) | MeetingScheduler can create bookings with internal types (teacher_interview, admin_checkin | src/app/[lang]/admin/actions.ts:695-706 — restrict createAdminBooking to server-side allowlist (class, placement_test). Model internal meeti |
| ◻ | ADMIN-04 | medium | Admin (2nd pass) | Admin assignment paths never verify the teacher is is_active server-side | src/app/[lang]/admin/actions.ts — create shared requireActiveTeacher(teacherId) that selects teacher row and rejects when is_active is false |
| ◻ | CALL-01 | medium | Call room (2nd pass) | Data-channel control events trust any sender — forged {type:'ended'} kicks the student | src/app/[lang]/sala/[bookingId]/components/RoomShell.tsx:97-104, 160-167, 196-204 — gate each callback on msg.from?.identity: honor session- |
| ◻ | STRIPE-01 | medium | Payments / Stripe (2nd pass) | Webhook idempotency ledger claimed before processing, never released on student-not-found  | src/app/api/stripe/webhook/route.ts:140 — set processingError so the release block runs and Stripe retries. For truly non-replayable cases ( |
| ◻ | STRIPE-02 | medium | Payments / Stripe (2nd pass) | Chargebacks never revoke credits — dispute → money back + classes kept, no alerting | src/app/api/stripe/webhook/route.ts:205-211 — on charge.dispute.created/.funds_withdrawn, reverse plan credits atomically (idempotent per ch |
| ◻ | LIVE-S06 | medium | Admin bookings calendar and library | Hardcoded English on /es (admin calendar + library chrome) | Move hardcoded strings into a localized 't' object (model: AdminLibraryClient.tsx:23-66). Define both EN and ES versions. Pass lang prop and |
| ◻ | E8-BILINGUAL-ASSIGNMENT-EMAIL | medium | Copy: sendAssignmentEmail hardcoded ES o | Teacher assignment email (admin → student/teacher) Spanish-only | src/app/[lang]/admin/actions.ts sendAssignmentEmail + sendBookingEmails: pass lang as parameter from calling context (available in assignAnd |
| ◻ | E9-BILINGUAL-APPROVAL-EMAIL | medium | Copy: approval + rejection emails hardco | Teacher approval/rejection emails (approveTeacherWithEmail/rejectTeacherWithEmail) Spanish | src/app/[lang]/admin/actions.ts approveTeacherWithEmail + rejectTeacherWithEmail: fetch teacher's preferred_language from profiles table alo |
| ◻ | ST-18 | medium | Student navigation and sidebar | Add a Support/Help path for students | If more prominence needed: add 'Help' nav item to studentNav in Sidebar.tsx, or add a help icon/modal in the top bar of dashboard pages. Cur |
| ◻ | TE-05 | medium | Availability (Disponibilidad) page — ava | Availability needs 'a couple more tools' — copy slots, recurring patterns, business hours, | Add to the bulk-add modal: (1) 'Copy from day' selector to duplicate an existing day's slots to other days; (2) 'Save as template' / 'Load t |
| ◻ | AD-04 | medium | Admin interface and actions | Make admin more powerful/complex — define which extra capabilities | Clarify product requirements: which of these should be admin-only? (1) edit plan pricing dynamically, (2) issue manual refunds outside cance |
| ◻ | AD-05 | medium | Admin tools / student & teacher detail p | Impersonate / 'log in as' a student or teacher — admin debug view | Implement impersonation via a new server action (e.g., startAdminImpersonation) that: (1) asserts admin role, (2) mints a short-lived sessio |
| ◻ | Auth-LOW-next-path | low | Auth & session | next= allows cross-role internal paths with no role scoping | src/lib/safeNext.ts or consumer sites. Add role parameter to safeNextPath() and scope next= to role-safe prefixes (e.g., allow /maestro only |
| ◻ | Auth-LOW-remember-me | low | Auth & session | Keep me logged in / Remember me checkbox decorative | Either remove the checkbox from UI or wire it to session persistence (e.g., set a longer maxAge cookie on true). |
| ◻ | Auth-LOW-email-enum | low | Auth & session | Email enumeration on signup — distinct error message for already-registered | Business decision required: either return a generic message for all signup failures or accept the enumeration trade-off for UX clarity. |
| ◻ | Payments-LOW-stripe-errors | low | Payments / Stripe | Raw Stripe error messages surfaced to client | Catch errors and return a generic localized message (e.g., {error: lang==='es' ? '...' : '...'}); log raw error server-side. |
| ◻ | Payments-LOW-lang-param | low | Payments / Stripe | lang interpolated into success/cancel URLs unvalidated | Add guard: `const safeLang = lang === 'en' ? 'en' : 'es'` before interpolation. |
| ◻ | Payments-LOW-checkout-ratelimit | low | Payments / Stripe | No rate-limit or double-submit protection on checkout creation | Add per-user rate limit (e.g., max 1 checkout per minute) or use Stripe's idempotency_key parameter in the session.create() call. |
| ✓ | BOOKING-05 | low | Student dashboard & nav (LOW), Booking f | Conflict check uses exact timestamp equality; offset-start bookings overlap undetected | src/app/actions/booking.ts:118-124 and placement.ts:68-74: Use an interval-overlap query: SELECT ... WHERE student_id=X AND status != 'cance |
| ✓ | BOOKING-06 | low | Booking flow (LOW) | duration_minutes client-controlled and unvalidated | src/app/actions/booking.ts:82: Whitelist duration_minutes to [30, 60, 90] (or whatever set is intended). |
| ◻ | BOOKING-07 | low | Booking flow (LOW) | No upper bound on future booking date; far-future bookings consume non-expiring credits | src/app/actions/booking.ts:87: Add maxAllowed = new Date(Date.now() + 90 * 24 * 60 * 60 * 1000) and check scheduledDate <= maxAllowed. |
| ✓ | BOOKING-08 | low | Booking flow (LOW) | Raw Postgres error (e.g., 23505 unique-violation) leaked to client despite pre-check | src/app/actions/booking.ts:148: Detect error.code === '23505' or error.message.includes('duplicate') and return a localized message like the |
| ◻ | BOOKING-09 | low | Booking flow (LOW) | confirmBooking returns success on zero-row match (WHERE clause blocks write but action suc | src/app/actions/booking.ts:185-191: Add `.select().single()` to the update chain to fetch the updated row and assert it exists; if single()  |
| ✓ | TEACH-LOW-1 | low | Teacher (maestro) | updateTeacherProfile lacks a teacher role check | Add after line 86: const { data: teacher } = await admin.from('teachers').select('id').eq('profile_id', user.id).single(); if (!teacher) ret |
| ◻ | TEACH-LOW-2 | low | Teacher (maestro) | Earnings always shown in USD | Import useCurrency hook, replace formatUsd calls with currency-aware formatter, or explicitly label 'USD' + provide switcher. |
| ◻ | TEACH-LOW-4 | low | Teacher (maestro) | completeSession logs raw errors/ids; payout fallback mis-prices | Remove or downgrade console.log calls to debug level. Standardize fallback to 60 min. Flag/error if hourly_rate is null before computing ses |
| ◻ | ADMIN-08 | low | Admin CRM | Admin bookings UI hardcoded English strings everywhere | Create admin dictionary in translations.ts, swap hardcoded strings for localized keys, add locale-formatted labels. |
| ◻ | BROWSE-01 | low | Browse teachers (student dashboard) | Hardcoded English 'Your Teacher' fallback renders in ES locale when teacher name is null | src/app/[lang]/dashboard/maestros/page.tsx:415 — add yourTeacherFallback key to the t dictionary and use t.yourTeacherFallback instead of li |
| ◻ | ASSIGN-04 | low | Assignments / homework | gradeSubmission can grade a cancelled assignment (no status check) and re-grade indefinite | src/app/actions/assignments.ts:160 — add status to select; check: if (a.status !== 'open') return error before updating submission. |
| ◻ | ONBOARD-03 | low | Onboarding + Intake | completeTeacherOnboarding JSON.parse of client-supplied specializations/certifications ung | src/app/actions/onboarding.ts:58-59 — wrap in try/catch; validate Array.isArray, filter to non-empty strings, cap array length ≤20 and strin |
| ✓ | ONBOARD-04 | low | Onboarding + Intake | Teacher onboarding accepts CV by MIME/extension only — no magic-byte check | src/app/actions/onboarding.ts:72 — validate magic bytes (%PDF, PK\x03\x04, D0CF) from buffer before upload. |
| ◻ | ONBOARD-05 | low | Onboarding + Intake | Intake survey_answers-skip marks intake_done=true without populating structured columns | src/app/[lang]/dashboard/intake/page.tsx:27-33 — map placement answers into structured columns when skipping, or require dedicated intake su |
| ◻ | ONBOARD-06 | low | Onboarding + Intake | Onboarding/intake server-action error messages are English-only and leak raw Supabase erro | src/app/actions/onboarding.ts and intake.ts — return localized error codes keyed on lang; log raw error.message server-side, return generic  |
| ◻ | AUTH-04 | low | Auth & session (2nd pass) | Wrong-language flash (FOUC) on /en login, registro, reset — lang read in effect instead of | src/app/[lang]/(routes)/page.tsx — read lang synchronously: const { lang } = use(params); validate against locales, default 'es'; drop useSt |
| ◻ | BOOK-01 | low | Booking (2nd pass) | Default timezone diverges between Mis Clases (Bogota) and Agendar/Placement (Tegucigalpa)  | src/app/[lang]/dashboard/clases/page.tsx:57 — replace 'America/Bogota' with 'America/Tegucigalpa' to use one canonical fallback everywhere. |
| ◻ | BOOK-02 | low | Booking (2nd pass) | Admin notification emails format class time in server's local (UTC) zone, not Honduras | src/app/actions/booking.ts:35 and 336 — add timeZone:'America/Tegucigalpa' to toLocaleString calls, matching placement.ts:190-194. |
| ◻ | ADMIN-05 | low | Admin (2nd pass) | createAdminBooking accepts arbitrary past/far-future scheduled_at and unbounded duration w | src/app/[lang]/admin/actions.ts:695 — validate scheduledAt (not NaN, within sane future window), whitelist durationMinutes (30/45/60/90), va |
| ◻ | ADMIN-06 | low | Admin (2nd pass) | Admin overview 'Scheduled' times render in UTC timezone, not America/Tegucigalpa | src/app/[lang]/admin/overview/page.tsx:233-235 — add timeZone:'America/Tegucigalpa' to toLocaleString. |
| ◻ | ADMIN-07 | low | Admin (2nd pass) | addStudentClasses toast/write use Number(input) stale/0 value when cleared; two inputs sha | src/app/[lang]/admin/students/[studentId]/StudentProfileClient.tsx:511 — clamp to Math.max(1, Math.trunc(Number(v)||1)). Give inputs indepen |
| ◻ | CALL-02 | low | Call room (2nd pass) | Whiteboard IndexedDB persistence keyed only by bookingId leaks across users on shared devi | src/app/[lang]/sala/[bookingId]/components/Whiteboard.tsx:91 — scope persistenceKey by user.id, clear store on unmount/leave, or disable loc |
| ◻ | CALL-03 | low | Call room (2nd pass) | Transcript 'finals' rendered/persisted from unauthenticated wire data (identity/name spoof | src/app/[lang]/sala/[bookingId]/hooks/useLiveTranscript.ts:88-94 — set identity/name from msg.from; cap text per line (~500 chars) and total |
| ◻ | CALL-04 | low | Call room (2nd pass) | Admin presence disclosed to student via participant name '(Admin)' (no observer framing; h | src/app/actions/video.ts:168-170 — either deliberate observer UX (localized banner explaining class is observed), or hide admin observer wit |
| ◻ | CALL-05 | low | Call room (2nd pass) | Chat React key (timestamp, identity) can collide — dropped/duplicated bubbles | src/app/[lang]/sala/[bookingId]/components/ChatPanel.tsx:78 — key on stable unique id (msg.id or monotonic counter), not timestamp+identity. |
| ◻ | CALL-06 | low | Call room (2nd pass) | generateSessionSummary parses model output with JSON.parse and trusts shape — malformed JS | src/app/actions/video.ts:270 — validate parsed shape (arrays for covered/nextTopics, string progressNote) before persisting; coerce/fall bac |
| ◻ | CALL-07 | low | Call room (2nd pass) | getRoomAccess late-cap trusts scheduled_at with no NaN guard — malformed timestamp leaves  | src/app/actions/video.ts:123 — add: if (isNaN(scheduled)) return { error: '...' } before computing closeAt. |
| ◻ | STRIPE-03 | low | Payments / Stripe (2nd pass) | Stacking packs overwrites current_plan with most recently purchased (often smaller) plan | src/app/api/stripe/webhook/route.ts:114 — don't blindly overwrite. Keep highest tier, track plan history in payments table, or set current_p |
| ◻ | STRIPE-04 | low | Payments / Stripe (2nd pass) | createCheckoutSession returns hardcoded English 'Invalid plan' rendered verbatim to ES use | src/app/actions/stripe.ts:36 — return localized string or error code keyed on lang parameter, matching createStripeConnectLink pattern. |
| ◻ | LIB-01 | low | Library / biblioteca | deleteBook removes storage file BEFORE deleting DB row — orphaned catalog entry on partial | src/app/actions/library.ts:100-103 — delete DB row FIRST; remove storage object only after (best-effort, log failures). Or wrap both in RPC/ |
| ✓ | LIB-02 | low | Library / biblioteca | Book with CEFR level 'all' produces duplicate React keys and dead 'All levels' filter tab | src/app/[lang]/dashboard/biblioteca/BibliotecaClient.tsx:172-197 — namespace level keys (key={`lvl-${l}`}); decide product intent — exclude  |
| ◻ | LIB-03 | low | Library / biblioteca | uploadBook trusts client-supplied file.type for MIME validation (no magic-byte sniffing) | src/app/actions/library.ts:48-54 — validate PDF magic header (%PDF-) from already-read Buffer before upload; keep forced contentType; option |
| ◻ | LIB-04 | low | Library / biblioteca | setBookActive reports success on zero-row update (non-existent/foreign bookId) | src/app/actions/library.ts:78-85 — .select() on update and assert exactly one row affected; return 'Book not found' otherwise. |
| ◻ | LIB-05 | low | Library / biblioteca | getBookSignedUrl has no rate limiting and no enrollment/plan gate — unbounded 15-min signe | src/app/actions/library.ts:111-129 — decide product gate (active plan / classes_remaining>0, restrict teachers to approved); add per-user ra |
| ◻ | LIB-06 | low | Library / biblioteca | uploadBook applies no server-side length cap to title or description | src/app/actions/library.ts:57-62 — add server-side caps (title ≤120, description ≤1000) and reject/truncate; add maxLength to description te |
| ◻ | FOOTER-01 | low | Public landing / marketing | Footer language switch hard-coded to /es and /en root — toggling locale from subpage dumps | src/components/landing/Footer.tsx:221-243 — compute other-locale equivalent of current pathname (anchored regex replace of leading /es|/en)  |
| ◻ | LANDING-01 | low | Public landing / marketing | MorningBanner shows hardcoded fake timestamp '06:14 · Tuesday/martes' next to genuinely li | src/components/landing/MorningBanner.tsx:5,11 — drop day token (keep '06:14'), or derive weekday from real date in user's locale; if static, |
| ◻ | LANDING-02 | low | Public landing / marketing | Currency amounts grouped with runtime default locale, not page language (non-deterministic | src/lib/currencies.ts:63 — pass explicit locale derived from lang to toLocaleString inside price-specific formatter so SSR and client agree. |
| ◻ | LANDING-03 | low | Public landing / marketing | HeroBookingCard '5 free' live-availability badge is fabricated count under 'En vivo' heart | src/components/landing/HeroBookingCard.tsx:187-191 — replace bare number with non-numeric availability language or label it explicitly illus |
| ◻ | LANDING-04 | low | Public landing / marketing | FinalCTA logged-in CTAs hard-link to student routes for ALL roles | src/components/landing/FinalCTA.tsx:138-155 — pass role into landing components and target role-appropriate home (maestro/dashboard/admin);  |
| ◻ | LANDING-05 | low | Public landing / marketing | FinalCTA 'Free discovery class' CTA carries ?intent=discovery but /registro ignores it | src/app/[lang]/registro/page.tsx:164-179 — read intent=discovery to pre-select role='student' + discovery copy, or drop param so CTAs don't  |
| ◻ | PASSWORD-01 | low | Auth & session (2nd pass) | Password-reset completion hardcodes redirect to /{lang}/dashboard for all roles | src/app/[lang]/login/new-password/page.tsx:107 — after updateUser succeeds, fetch profiles.role and route accordingly (teacher→/maestro/dash |
| ◻ | PASSWORD-02 | low | Auth & session (2nd pass) | Reset-link token can be double-consumed by React effect re-run — spurious 'expired link' e | src/app/[lang]/login/new-password/page.tsx:46-90 — check getSession() first and skip exchange if session exists; de-dupe with ref so exchang |
| ◻ | STUDENT-DASHBOARD-01 | low | Assignments / homework | Assignment list pages bypass RLS by using service-role admin client for reads | src/app/[lang]/dashboard/tareas/page.tsx and maestro/dashboard/tareas/page.tsx — use RLS-scoped createClient() for reads so mig-017 policies |
| ✓ | HOMEWORK-01 | low | Assignments / homework | Student homework card renders due date in hardcoded Spanish locale regardless of UI langua | src/app/[lang]/dashboard/tareas/StudentTareasClient.tsx:390 — change to formatDate(a.due_at, lang). |
| ✓ | HOMEWORK-02 | low | Assignments / homework | Student 'Completed' section header miscounts (includes cancelled) vs stat tile (only submi | src/app/[lang]/dashboard/tareas/StudentTareasClient.tsx:144-145 — give cancelled its own section or exclude cancelled-without-submission; un |
| ◻ | LIVE-003 | low | Auth / login page | Logged-in user revisiting /login sees the form | Add server-side session check in login page or wrap the form in a client component that redirects authenticated users to their role-appropri |
| ◻ | LIVE-004 | low | Auth / login form | 'Keep me logged in' checkbox is decorative | Add name='keep_logged_in' to the checkbox at line 197, read it in signIn() action, and conditionally set ROLE_COOKIE_OPTS.maxAge (currently  |
| ◻ | LIVE-S05 | low | Admin overview/tables | toLocaleString() without explicit timeZone causes SSR/hydration mismatch | Add timeZone: 'America/Tegucigalpa' to all toLocaleString() calls in admin/overview/page.tsx and other admin components. Pattern already cor |
| ◻ | LIVE-S08 | low | Teacher availability weekly summary | '1 slots' — slot count not pluralized | Change slots definition to a function: slots: (n) => \`slot${n === 1 ? '' : 's'}\` (EN), slots: (n) => \`horario${n === 1 ? '' : 's'}\` (ES) |
| ◻ | LIVE-S09 | low | Auth / login page | Logged-in user on /login sees form (duplicate of LIVE-003) |  |
| ◻ | LIVE-S10 | low | Auth / login form | 'Keep me logged in' checkbox decorative (duplicate of LIVE-004) |  |
| ◻ | TE-D1 | low | Materials (Materiales) page | Set up 'materials' = the books/curriculum (deferred) | Design: (1) Create materials table in DB (id, title, type, file_path/url, teacher_id, created_at). (2) Choose hosting: Supabase Storage, AWS |
| ◻ | TE-D2 | low | Assignments (Tareas) page | Assignments end-to-end test (deferred — need real student) | This is a testing-only task: once a real teacher and student are paired with a confirmed booking, manually test the assignment creation → su |
| ◻ | AD-D1 | low | Admin biblioteca page (stub) | Library/books — deferred pending curriculum discussion (cross-cutting with student Library | No action required yet — design/scope library feature with Carlos, then implement admin CRUD (create, read, update, delete books; assign to  |
| ◻ | ST-02 | feature | Settings > Profile section — avatar uplo | Profile photo upload | Implement file upload handler in ConfigStudentClient line 599: call Supabase Storage bucket (create if missing at supabase/avatars/{user.id} |
| ◻ | ST-07 | feature | My Progress page — self-rated level opti | Self-declared level (skip diagnostic testing) | Add a collapsible/toggle section in ProgresoClient.tsx (before the 'Current level' banner, around line 480) offering students to input self- |
| ◻ | ST-09 | feature | My Progress page — 'Your learning profil | Edit the Learning Profile affordance | Add an edit button/icon in the profile block header (after line 765) linking to /dashboard/intake or a new /dashboard/progreso/edit page, or |
| ◻ | ST-12 | feature | My Progress page or a new Certificates s | Student diploma / certificate | Design scope: (1) Legal research — what 'certified' means for EnglishKolab (accreditation, CEFR levels?); (2) Certificate design — template/ |
| ◻ | AD-01 | design | Admin pages (overview, students, teacher | De-AI the entire admin section — replicate Cálido Editorial system | BookingCalendarClient needs refactoring to use Cálido Editorial design tokens and component library instead of inlined CSS. Extract color/sp |

## Needs live-check (probe the running app to confirm)

| ✓ | id | sev | surface | item | fix hint |
|---|---|---|---|---|---|
| ◻ | Payments-MEDIUM-post-checkout-success | medium | Payments / Stripe | Post-checkout success screen fabricates classes added from URL params | Fetch real classes_remaining from DB server-side on the success page; render real balance, not client-computed value from URL. |
| ◻ | LIVE-SALA-MED-3 | medium | Live call (sala) | Booking not completed if teacher's browser drops after disconnect | Verify: does completeSession fire via sendBeacon on pagehide? Is there a server-side fallback for orphaned 'confirmed' bookings? Trace the f |
| ◻ | TEACH-MED-3 | medium | Teacher (maestro) | Admin/teacher notification emails inject unescaped names/reasons into HTML | Search codebase for all email templates in booking.ts, placement.ts, and admin actions.ts. Wrap all user-supplied full_name, notes, reason w |
| ◻ | TEACH-MED-4 | medium | Teacher (maestro) | Decline / student-cancel can double-refund under concurrency | Trace both paths (declineBooking + studentCancelBooking). Refund only if UPDATE matches exactly 1 row: UPDATE bookings SET status='cancelled |
| ◻ | E11-SUPABASE-AUTH-EMAILS | medium | Branding/copy: Supabase's own mailer for | Supabase-templated auth emails (signup confirmation + password recovery) | Manual action (Carlos/QA): Log into Supabase dashboard → Auth → Email Templates. Check Signup confirmation + Password recovery templates for |
| ◻ | ST-16 | medium | My Classes page — display how early a st | Surface the early-join window | Add a help text or info tooltip near Join button in ClasesClient.tsx explaining: 'You can join up to 24 hours before class and stay up to 90 |
| ◻ | Auth-LOW-callback-token | low | Auth & session | auth_callback_failed token not localized | OAuth callback route. Map auth errors to localized copy via translations dict before rendering. |
| ◻ | LIVE-SALA-LOW-1 | low | Live call (sala) | getRoomAccess/ErrorScreen messages English-only | Check src/app/[lang]/sala/[bookingId]/ErrorScreen.tsx. Likely needs: return stable error codes from getRoomAccess, map to localized strings  |
| ◻ | LIVE-SALA-LOW-2 | low | Live call (sala) | No early lower-bound on join — token mintable 24h+ early | If early-join gate is desired: add lower-bound check at line 136-147 similar to the closeAt logic. |
| ◻ | LIVE-SALA-LOW-3 | low | Live call (sala) | transcribeAudioChunk no rate limit / size cap | Add upper byte cap (e.g., 5MB) and per-booking throttle (e.g., max 60 chunks/min) if risk assessment warrants it. |
| ◻ | LIVE-SALA-LOW-4 | low | Live call (sala) | Chat/data-channel payloads not length-bounded or schema-validated | Inspect ChatPanel, RoomShell, Whiteboard for cap chat length, allowlist emoji, validate whiteboard records, rate-limit events. |
| ◻ | TEACH-LOW-3 | low | Teacher (maestro) | Inconsistent no-teacher-row redirect target | Inspect src/app/[lang]/maestro/dashboard/layout.tsx and sub-route page.tsx files. Standardize to one canonical redirect (likely /onboarding  |
| ◻ | LK2-31 | low | Typography balance across EN/ES translat | EN copy balance — longer EN strings (HowItWorks step 4, Pricing labels) uneven at 4-up; re | If uneven: add `min-height` to card title container or reserve a fixed-height band in .lk-how-card / pricing card grid. |

## Needs you — design judgment

| ✓ | id | sev | surface | item | fix hint |
|---|---|---|---|---|---|
| ◻ | E10-BRANDED-EMAIL-ADOPTION | medium | Branding/design: reminder/confirmation/p | brandedEmail() wrapper adoption (only welcome email uses it) | Batch E2 work: migrate all email bodies through brandedEmail({heading, bodyHtml, ctaLabel, ctaUrl, footnote}) template. Requires visual desi |
| ◻ | ST-01 | design | Settings / Configuración page — Profile, | De-AI the whole student Settings page | Review the live rendered Settings page at /dashboard/configuracion in both EN/ES; compare visual treatment (typography, spacing, color, tone |
| ◻ | ST-10 | design | My Progress page — layout/hierarchy | Reorder My Progress — surface recent classes + weekly count near top | If reordering is desired: (1) Calculate upcomingClasses/completedThisMonth from props; (2) render a new 'This week' metric near top; (3) con |
| ◻ | ST-14 | design | Scheduling & Classes page — diagnostic-c | Replace cheap 'bootstrap-y' icons | Visual audit: compare current icons against Cálido Editorial icon library / brand system. If lucide set is unsuitable, source bespoke SVG ma |

## Done (verified present in current code)

105 items confirmed fixed. Highlights: signup priv-esc, webhook amount-check, rate-limiter XFF+lockout, RLS confidentiality (031/033), admin/conductor room-join, extractLiveVocab lock, handle_new_user trigger, Email Batch E1, plus most LIVE-S0x and ~43 medium audit findings.
